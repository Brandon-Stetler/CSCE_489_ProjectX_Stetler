# On-Device Continual Learning (CPU-only demo)

Goal: show whether a resource-constrained robot can refine a small vision model **on board** (no cloud) within tight CPU/RAM/time budgets.

**Success bars**
- Accuracy: ≥80% on 20 real photos **and** +15 pp over pre-tune
- Effort: total fine-tune <60 s, peak RAM <400 MB, no paging

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip -r requirements.txt
sudo apt install -y linux-tools-common linux-tools-generic  # for 'perf' (optional)